To reset your password, click the URL below. This url is valid for 15 minutes.

<%= Rails.application.config.email_url %>/Reset/<%= @user.password_reset_token %>

If you did not request your password to be reset, just ignore this email and your password will continue to stay the same.
